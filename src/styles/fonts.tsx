import { Hanken_Grotesk, Inter, Literata } from "next/font/google";

export const inter = Inter({ subsets: ["latin"] });

export const grotesk = Hanken_Grotesk({ subsets: ["latin"] });

export const literata = Literata({ subsets: ["latin"] });
